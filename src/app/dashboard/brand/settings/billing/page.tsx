import BillingSettings from '@/components/BillingSettings';

export const metadata = { title: 'Facturación — Connectly' };

export default function BrandBillingPage() {
  return <BillingSettings role="brand" />;
}
